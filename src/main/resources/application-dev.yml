spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_dev?useSSL=false
    username: sfg_dev_user
    password: guru
    initialization-mode: ALWAYS
   # platform: 
     
  jpa:
    hibernate:
      ddl-auto: validate
      
    database-platform: org.hibernate.dialect.MySQL8Dialect
   # database-platform: org.hibernate.dialect.storage_engine=innodb
    database: MYSQL
    show-sql: true
    properties: 
      javax:
        persistence:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: guru-database-create.sql
              
    